
// Grab everything from the local Header file that is being exported, re-export it out as Header
export {default as Header} from './Header';
export {default as Nav} from './Nav';
export {default as Main} from './Main';
export {default as Footer} from './Footer';

// The variable is in the curly braces and file name is on the right
// The string must match name of file. variable name can be anything
